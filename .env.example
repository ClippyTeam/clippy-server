CLIPPY_TOKEN=change_me_to_a_long_random_string

# Keep last N clips
CLIPPY_RETENTION=200

# Max payload size in bytes (base64 decoded)
CLIPPY_MAX_BYTES=65536

# Default TTL seconds (24h)
CLIPPY_DEFAULT_TTL=86400

# DB path in container
CLIPPY_DB=/data/clippy.db
